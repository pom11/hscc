name: hscc-commands
version: 1.0.0
description: "Operator slash commands for cluster incident response — /orch-restart, /cluster, /cluster-restart. Direct sparkrun handlers (run in the gateway, not via the LLM) so they work even when the orchestrator model is wedged."
author: NousResearch
kind: backend
provides_commands:
  - orch-restart
  - cluster
  - cluster-restart
