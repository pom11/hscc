# memori_byodb

Bring-your-own-DB memory provider (the variant HSCC uses): a NAS-backed memory
store with offline local-LLM augmentation, wired via `memory.provider:
memori_byodb` + env in the gateway plist. Sibling of `memori/`.
