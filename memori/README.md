# memori

Memory-provider plugin for Hermes (agent long-term memory). Used by Hermes'
`memory.provider` config. See `memori_byodb/` for the bring-your-own-DB variant
HSCC runs (NAS-backed store + offline local-LLM augmentation).
