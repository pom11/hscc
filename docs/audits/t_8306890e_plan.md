# t_8306890e — Dispatcher worktree placement fix (configurable root + relocate EcoFire worktrees)

## Problem
The kanban dispatcher creates worktrees at `<repo>/.worktrees/<id>` (hardcoded in
hermes-agent `hermes_cli/kanban_db_workspace.py::_anchored_worktree`). For
/Users/desac/dev/EcoFire_customizations_bc the project root IS the AL compiler root,
so the compiler walks the `.worktrees/` dirs and compiles half-finished branch code:
baseline 2487 files, 1,991 of them worktree noise, AL1021 error. `.gitignore` does
NOT fix this (git ignoring a path does not stop a compiler walking the filesystem),
and AL has no app.json exclude key. The fix is PLACEMENT.

## Current state (verified 2026-09-24)
- `git worktree list` in EcoFire shows 4 in-tree worktrees + main tree:
  - .worktrees/ctw1        [wt/t_d4705029]   @ 81a76a1
  - .worktrees/t_49627a43  [feat/transfer-stock-fix] @ bae45d0
  - .worktrees/t_4dbda030  [wt/t_4dbda030]   @ 864d6a8
  - .worktrees/t_c27d531b  [wt/t_c27d531b]   @ 51725f0
- Main tree HEAD f08f48e [feat/wcollect-descr-pr]
- EcoFire .gitignore does NOT contain .worktrees/ → must add.

## Risk: LOSING UNMERGED BRANCH WORK IS THE ONE UNACCEPTABLE OUTCOME
- ctw1 (wt/t_d4705029) and t_49627a43 (feat/transfer-stock-fix) carry unmerged branch
  work. Use `git worktree move`, NEVER rm.

## Plan
A) hscc-side hermes patch (this worktree, branch wt/t_8306890e → main → push):
   1. Modify hermes-agent source: kanban_db_workspace.py `_anchored_worktree` +
      fallback to consult `kanban.worktree` config (out_of_tree_repos list +
      out_of_tree_root base dir).
   2. Capture as git format-patch into patches/hermes/00012-*.patch.
   3. Update patches/MANIFEST.md.
   4. Suite green on 8 packages under BOTH interpreters.
   5. run python3 hscc-bootstrap/install_payload.py after merge.
B) Operational relocation (verified by execution):
   1. `git worktree move` all 4 to ~/dev/.worktrees/EcoFire_customizations_bc/<id>.
   2. Add .worktrees/ to EcoFire .gitignore.
   3. Re-run AL compile, show file count drops below 2487.
   4. Check other registry repos for same exposure.

## Exposure check (other registered repos) — verified
Registry: ~/.flightdeck/registry.yaml. Repos with .worktrees/ inside a tree where a
compiler/bundler would walk them:
- hscc (Python) — pytest norecursedirs excludes .worktrees/, harmless. NOT unsafe.
- EcoFire_customizations_bc — AL compiler root (app.json), THE mandated case. UNSAFE.
- ecofire (/Users/desac/dev/ecofire) — Flutter app (pubspec.yaml, lib/, build/, web/).
  Flutter analyze/build walks the whole tree for .dart files → 10 in-tree .worktrees/
  are EXPOSED. Same class of bug. UNSAFE.
- efsdriver (/Users/desac/dev/efsdriver) — Flutter app too (pubspec.yaml, lib/,
  android/). 1 in-tree .worktrees/ EXPOSED. UNSAFE.
- flosana — static docs/index.html, .worktrees/ empty, no compiler. Not unsafe.
- sphoin/soconn/ecofire-powerbi/Grid/Radio/pickolo/pom — no .worktrees/. Not exposed.

Decision: register EcoFire_customizations_bc + ecofire + efsdriver in
kanban.worktree.out_of_tree_repos so ALL FUTURE dispatches place their worktrees out
of tree. Relocate ONLY the 4 mandated EcoFire worktrees now (the other repos' existing
.worktrees/ belong to boards that may have running cards; leave in place, flag as
follow-up so we don't yank a live worker's CWD).

## Action log (run 2026-09-24)
Code (hermes patch 0012) committed to deployed hermes-agent as febbef720d; patch
captured as patches/hermes/0012-*.patch, verified to apply cleanly onto pristine
v2026.9.14 after 0001-0011 (git apply sequence over a fresh clone — all 9 OK).
patches/MANIFEST.md + CHANGELOG updated.
enable_plugins.py now registers EcoFire_customizations_bc + ecofire + efsdriver in
kanban.worktree (fill-only); 3 new tests; doctor fresh-config noop updated.

OPERATIONAL RELOCATION (done, verified by execution):
  git worktree move .worktrees/{ctw1,t_49627a43,t_4dbda030,t_c27d531b}
      -> ~/.dev/.worktrees/EcoFire_customizations_bc/<same-name>
  Verified: git worktree list shows all 4 OUTSIDE the project root; each still on
  its branch at its original commit (81a76a1 [wt/t_d4705029], bae45d0
  [feat/transfer-stock-fix], 864d6a8 [wt/t_4dbda030], 51725f0 [wt/t_c27d531b]);
  .worktrees/ removed from project root (empty rmdir).
  .gitignore in EcoFire_customizations_bc now ignores .worktrees/ (committed 58ab75c).
  .al file count in project root: 2487 -> 500 (0 .worktrees dirs remain). The AL
  compiler enumerates .al files across the tree, so it now sees 500, not 2487.

SCOPE DECISION: ecofire + efsdriver registered out-of-tree so FUTURE dispatches are
safe. Their EXISTING .worktrees/ NOT relocated now (belongs to boards that may have
running cards — moving would yank a live worker's CWD); flagged as follow-up task.

BLOCKER (config): ~/.hermes/config.yaml is a protected agent-instruction file —
direct write of the kanban.worktree block was blocked (no user to approve headless).
enable_plugins.enable() (the sanctioned bootstrap writer) is the intended path and
runs on next bootstrap.sh; install_payload.py only copies files, does NOT touch
config. Surfaced to operator after merge+push.

## Config shape (decided)
Under ~/.hermes/config.yaml:
  kanban:
    worktree:
      # Repos whose project root is itself a build/compiler root — in-tree
      # .worktrees/ under them get compiled as part of the app. Their worktrees
      # are placed OUT of tree under out_of_tree_root/<repo-name>/<task-id>.
      out_of_tree_repos:
        - /Users/desac/dev/EcoFire_customizations_bc
      out_of_tree_root: ~/dev/.worktrees
Matching is done by repo_root prefix comparison (same as existing .worktrees
detection). Default (no config) = in-tree .worktrees/, fully backwards compatible.
