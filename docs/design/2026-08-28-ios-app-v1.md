# iOS operator console — v1 design

Status: approved in brainstorming 2026-08-28. Supersedes the ad-hoc app built
card-by-card through 2026-08-27.

## Why this exists

The app connects and compiles, but it cannot do the one job that matters:
hold a conversation with a project's orchestrator. The operator's verdict was
that it "cannot replace the current Telegram way of chatting in each project".
This design makes the app the primary chat surface and raises the rest to a
standard that can be sold to other HSCC operators.

## Decisions (locked)

1. **Telegram is replaced.** The app talks to Hermes directly, in each
   project's own session.
2. **Audience: other HSCC operators.** Strangers install it against their own
   cluster. Onboarding, multi-cluster and legible errors are requirements, not
   polish.
3. **Chat shows live activity** — streaming tokens plus visible tool calls.
4. **hscc-api bridges to the Hermes gateway.** One endpoint, one bearer token.
5. **No push notifications.** Jobs run server-side; the reply waits in the app.
6. **Project-first IA** — project list, then Overview / Chat / Board / Settings.
7. **Every existing surface is kept and fixed** — but Siri, the widget and the
   Live Activity go LAST, as they have never executed once.

## 1. Chat architecture

Today `hscc-api` shells `hermes chat -Q --continue <session>` and scrapes
stdout. It cannot stream, cannot show tool activity, cannot create a session,
and reports every failure as one misleading string.

    iOS  --WSS/tailnet-->  hscc-api  --WS/loopback-->  hermes serve  -->  <project> session
         (one bearer token)          (holds gateway credential)

`hermes serve` is Hermes' JSON-RPC/WebSocket gateway (default :9119),
documented as the interface for "the desktop app and remote clients".

Consequences:
- the app joins the SAME session with the full agent loop — tools, kanban,
  memory — not a parallel one-shot channel
- streaming and tool events arrive as gateway events, not scraped text
- Hermes stays on loopback; only hscc-api is exposed on the tailnet
- jobs live server-side, so backgrounding the app cannot lose a reply

Two defects fixed here:
- **session bootstrap** — `--continue` resumes but never creates, so a fresh
  project can never be chatted with. The bridge creates on first message.
- **error attribution** — `if proc.returncode != 0` currently reports
  "session not ready (create it first)" for EVERY failure, discarding real
  stderr. This sent the operator (and the author of this design) chasing a
  missing session that existed. Failures must name themselves: model
  unreachable, session missing, timeout after N seconds.

**Unproven dependency.** `hermes serve` exists and is documented, but its event
schema and auth handshake have NOT been verified. If it cannot emit tool
events, the streaming promise degrades to text-only. A spike proves this
BEFORE anything is built on it.

## 2. Information architecture

Root: a project list. Inside a project: Overview / Chat / Board / Settings.
Cluster (nodes, GPU, wedges, autodown, templates) is a separate root surface —
it is about the fleet, not any one project.

- **Project row** — name, one health glyph, and the single fact that decides
  whether you tap: reply waiting, cards needing review, or last activity.
- **Overview** — answers "what is the state of this project?" in ~3 seconds:
  running/ready/blocked counts (blocked most prominent), repo branch and
  ahead/behind/dirty, session health and compaction headroom, and a tappable
  last-reply preview. The current top section renders wrong and is undiagnosed.
- **Chat** — full session history (not just this launch), streaming replies,
  collapsible inline tool activity, resend on failure, composer state that
  survives backgrounding.
- **Board** — the project's kanban: filter, open a card, read comments,
  recover a blocked one. Mutations stay confirm-gated.
- **Settings (project)** — resolved profile/session/board, model override.
  Connection and pairing move up to app-level settings.

**Shared foundation.** Each view was built by a different card in isolation,
which is why the app reads as assembled rather than designed. One theme applied
consistently, one loading/empty/error component set, one row style, one spacing
scale — enforced across every surface including the peripheral ones.

## 3. Sellable to other operators

- **First run** — QR pairing is the primary path, manual entry the fallback.
  A failed scan says why: wrong version, unreachable host, rejected token.
- **Multiple clusters** — host/port/token become a list of saved clusters with
  a name, health dot and last-connected. Also makes the app demoable.
- **Errors name the cause and the fix.** Never "Set a host, port, and token
  first" when all three are set.
- **Distribution** — TestFlight first; App Store is a later decision.
  Sideloading via Xcode does not scale past one person.
- **Security** — the QR carries a live credential: warn at pairing, support
  token rotation from the CLI, and state plainly in the docs that the tailnet
  is the security boundary.

## 4. Verification

Four bugs reached the operator on 2026-08-28 that local checks were
structurally incapable of catching:

| Bug | Why it was missed |
|---|---|
| `SetupQRCode` not in scope | compiled `find`-all, not the project's file list |
| `SettingsStore` self-before-init | `-typecheck` cannot see SIL diagnostics |
| Siri two-parameter phrase | Xcode's App Intents metadata processor only |
| Keychain token never saved | pure runtime |

`check_sources.sh` and `build_check.sh` now cover the first two. The operator
has declined installing the iOS simulator runtime, so **runtime verification is
not possible on the build host** — the App Intents and runtime classes have no
automated gate at all, and the operator's device is the only one.

Given that, the automatable win carries more weight:

- **Contract tests** exercising EVERY route the app calls with the app's exact
  payloads. Tonight's chat failure was a route/flag mismatch — the real route
  409s without `confirm` — which this class of test catches in CI rather than
  on a phone.
- **Device smoke checklist** for what cannot be automated: pair by QR, send a
  message, background mid-reply, scan with a wrong token.
- **Honesty rule** — anything unexecuted is labelled unexecuted, in cards and
  in release notes. No "verified" for something that has only compiled.

## Build order

1. Spike: prove the `hermes serve` event schema and auth
2. Chat pipeline (bridge, streaming, session bootstrap, error attribution)
3. Projects list + Overview + shared design foundation
4. Onboarding, multi-cluster, error legibility
5. Verification harness (contract tests, smoke checklist)
6. Siri, widget, Live Activity — last, never yet executed

## Out of scope for v1

Push notifications. App Store submission. Any backend other than HSCC.

---

# EXECUTION PLAN (autonomous, 2026-08-28 night)

The operator is asleep. This section is the contract for what gets built and how
it is reviewed, so nothing is lost between sessions.

## Authority granted
- modify HSCC, the cluster, AND Hermes itself (create profiles, crons, hooks)
- merge to `main` continuously as work passes review; the operator wakes to
  something pullable
- autodown stays ARMED at 120 min (a full board keeps the fleet busy; its
  interlocks refuse teardown while cards are running/ready)
- if the spike fails, DEGRADE chat to polling, keep the UI, and report — do not
  stall the plan

## All ten locked decisions
1. Telegram is replaced; the app talks to Hermes in each project's own session
2. Audience: other HSCC operators (onboarding, multi-cluster, legible errors)
3. Chat shows live activity — streaming tokens AND visible tool calls
4. hscc-api bridges to the Hermes gateway; one endpoint, one bearer token
5. No push; jobs run server-side, the reply waits in the app
6. Project-first IA: list -> Overview / Chat / Board / Settings
7. Every surface kept and fixed; Siri/widget/Live Activity LAST
8. Chat has FULL agent authority (creates cards, dispatches, spawns subagents)
9. Chat is a WINDOW onto the session (cron firings, worker reports, card moves)
10. No iOS simulator runtime — runtime classes have NO automated gate

## Waves and dependencies

**Wave 0 — blocking spike** (ready now)
- t_74106da7 SPIKE: hermes serve schema/auth/session-subscription
Everything in Wave 1 depends on its answer.

**Wave 0b — independent fixes** (ready now, no dependency)
- t_201ffe7d error attribution (every failure reported as "session not ready")
- t_8901cecd broken Projects header
- t_6728c271 contract tests (the one automatable gate for the app/API seam)

**Wave 1 — chat pipeline** (promote when the spike reports GO/DEGRADE)
- t_47f51a71 WebSocket bridge
- t_fc53b13d create session on first message
- t_1ff4dcbd streaming chat view with typed events
- t_218cb9ec reconnect without gaps/duplicates
- t_c0953d4c unsent/retry + honest terminal states
- t_2776ea3c session history paging
- t_a779c06f screenshot attachments
- t_267da363 per-project unread badges

**Wave 2 — IA and craft**
- t_c16a1ae9 shared design foundation (the card that decides if it looks like a product)
- t_2f2f301a Project Overview
- (Projects header fix already in 0b)

**Wave 3 — Hermes control plane**
- t_740d9489 profile lifecycle endpoints
- t_dafe62bc profile editor per project
- t_3954bf21 profile library (install from git URL)
- t_9a5cfc3b approvals inbox
- t_9e678c54 kill switch
- t_c2631579 sessions manager
- t_e8ffd787 memory viewer

**Wave 4 — HSCC surfaces (mostly wrapping existing CLI)**
- t_f22be3c4 hscc project portfolio (standup/qa/review/roadmap/incident/release/why)
- t_cd08c30a activity + cost per project
- t_308fce41 live agent activity feed
- t_1d7080ff triggers + escalations

**Wave 5 — sellable**
- t_f0af3d6a QR-first onboarding
- t_4817c04a multiple saved clusters
- t_d07e0605 error legibility pass
- t_7f90dbb7 new-project bootstrap wizard

**Wave 6 — verification**
- t_dce08413 device smoke checklist

**Wave 7 — peripherals, LAST by operator decision**
- t_e9833a8b Siri, t_ae387855 widget, t_57c3779c Live Activity

## Review protocol (every card, no exceptions)
Verify by EXECUTION, never by reading the worker's report:
- iOS: check_sources.sh + build_check.sh exit 0; model_decode_check.sh if models
  changed; no raw hex outside Theme.swift
- Python: run_tests.sh green; hash ~/.hscc/{autodown,serving,api}.json and
  ~/.hermes/profiles/*/config.yaml before/after
- claims are checked against the code, not believed. A worker's assertion that a
  constant is "imported, not duplicated" held only because it was checked.
- merge dev->main, push, CONFIRM origin
- DEPLOY when it changes runtime behaviour: `bash hscc-bootstrap/bootstrap.sh
  --yes`, then confirm the feature is in the INSTALLED payload, then re-run the
  post-bootstrap checklist (autodown setting, max_concurrent_children=9, every
  profile threshold_tokens=100000)

## Known traps (hard-won, do not relearn)
- merging is NOT deploying; the daemon runs ~/.hermes/plugins/
- "rc=0 with no worker comment" = the ENVIRONMENT failed, not the worker
- a card `running` with no live process is stale -> kdb status='ready'
- a mid-run worker does NOT re-read comments; corrections reach the NEXT run only
- `hermes kanban create` is broken (NOT NULL: tasks.session_id) -> kdb.create_task
- workers may land work on a plain branch, not wt/<id>
- health = REAL GENERATED TEXT. GPU%, /v1/models listings and 200s have all lied
- proxy is MAC-LOCAL 127.0.0.1:4000; the API binds the TAILNET address only
- concurrency: 3 GB10 workers saturate at ~6 concurrent cards

## Definition of done for v1
A stranger installs the app, scans the QR, picks their cluster, opens a project,
chats with its orchestrator and watches it create cards — and never sees an
error that does not tell them what to do about it.


---

## SPIKE RESULT (2026-08-28 20:15) - GO with one amendment

The spike card completed with NO report; answered directly against the Hermes
source. Evidence is file:line in `hermes_cli/web_server.py`.

**GO:** streaming replies and inline tool activity are achievable. `hermes serve`
is a FastAPI server (:9119). Chat is `/api/pty` (:14125), a PTY-over-WebSocket
ANSI bridge. Structured tool events flow over `/api/pub` -> `/api/events`
(:15594): the PTY-side gateway writes "every dispatcher emit" to a channel and
the server fans it out to subscribers on the same channel id - this is what feeds
the dashboard's own tool-call sidebar. `_DASHBOARD_EMBEDDED_CHAT_ENABLED = True`
(:325), so no flag is needed.

**AMENDMENT to decision 9.** "Chat is a window onto the session" cannot be
satisfied by the gateway. The channel is bound to a PTY session the client itself
starts; events produced outside it - cron firings, kanban workers, card moves -
never reach it. Those must be merged into the same timeline from a SECOND source
(the kanban DB and the daemon check streams). The user-visible design is
unchanged; the implementation has two producers, not one.

**Also:** the broadcast carries no sequence numbers, so the bridge must add them
for gap-free reconnect. Session creation is not offered by the gateway either, so
t_fc53b13d (create-on-first-message) remains necessary on its own.

---

## OVERNIGHT RUN 2026-08-29 → 08-30 — chat pipeline COMPLETE

Everything in waves 90–40 landed and is on `origin/main` and deployed. Board:
114 done, only the two operator-deferred peripherals remain (widget, Live
Activity), now unparked and running. Suite grew 390 → **503 hscc-api tests**.

**What shipped, in dependency order**

| commit | card | what |
|---|---|---|
| `7a7c5b7` | t_201ffe7d | chat failures name themselves instead of every error reading "session not ready" |
| `217e6af` | t_6728c271 | contract gate deriving routes FROM the Swift client so app↔server cannot drift |
| `f4a65fc` | t_47f51a71 (inc 1) | `session_event.py` — the pinned wire contract + history endpoint |
| `8281698` | t_267da363 | per-project unread badges (+ fixed a dev-breaking SettingsView regression) |
| `29174e8` | t_740d9489 | `/v1/profiles/*` lifecycle + config endpoints |
| `257bee7` | t_2f2f301a | Project Overview answering "what is the state of this project?" |
| `19d9a08` | t_dafe62bc | per-project profile editor (API + client + UI) |
| `529ff1f` | t_2776ea3c | session history paging + a 3-site latent query-param fix |
| `77189ea` | t_47f51a71 (inc 2) | RFC6455 framing, store fan-out, live WS endpoint |
| `38183ae` | t_1ff4dcbd | streaming chat view with typed events |
| `6b42b22` | t_776e294a | hermes-serve gateway driver — real activity now feeds the stream |

**The chat pipeline is end to end**: history and the live socket share ONE
contiguous seq space per project, so the app pages history down to `seq` then
subscribes from `seq+1` with no gap and no duplicate; the driver translates
native `/api/pty` + `/api/events` frames into the same `session_event` shapes.

**Not verified, and not verifiable on this host:** nothing was built or run on
an iOS device or simulator. Compile, source registration and fixture decoding
are proven; runtime layout and App Intents / extension metadata are NOT. The
device smoke checklist remains the gate for those.

**Operational lessons worth keeping** (all cost real time tonight)

- Cards were invisible to the dispatcher because of **stale `claim_lock` rows**
  whose `claim_expires` had passed; the reclaim pass only clears claims on
  `running` tasks, never `ready` ones.
- Kanban caps are **snapshot at gateway startup**; `hermes kanban dispatch
  --dry-run` reads config fresh, so the two disagree and the dry-run lies.
- Throughput ceiling is the **serving unit**, not the worker count — all workers
  share one TP pair, so extra width only deepens the vLLM queue.
- Resetting a card to `ready` can spawn **two workers on one card** that clobber
  each other's files. Check `ps ... | uniq -c` after any manual recovery.
- A worker may check its branch out in the **main checkout**; cherry-picking
  there lands on its branch, and switching would destroy its uncommitted work.
- Six cards were `blocked` while holding complete, shippable commits. Blocked is
  usually a deliberate checkpoint — read the branch before believing a failure.
